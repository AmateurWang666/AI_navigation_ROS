# 本仓库在 Windows 编辑、在 WSL/Linux 构建运行。core.autocrlf=true 时，
# 若没有这里的规则，检出会把 shebang、YAML、xacro 变成 CRLF，导致
#   wsl bash ./scripts/sync_ws.sh
# 或 roslaunch 解析直接失败。以下类型在索引与工作区都固定为 LF。

* text=auto

*.sh text eol=lf
*.py text eol=lf
*.yaml text eol=lf
*.yml text eol=lf
*.xml text eol=lf
*.cfg text eol=lf
*.md text eol=lf
*.launch text eol=lf
*.urdf text eol=lf
*.xacro text eol=lf
*.world text eol=lf
*.sdf text eol=lf
*.config text eol=lf
*.txt text eol=lf
CMakeLists.txt text eol=lf
.gitignore text eol=lf
.gitattributes text eol=lf

# 网格与贴图是二进制，不要做行尾转换
*.STL binary
*.stl binary
*.dae binary
*.jpg binary
*.png binary

# 栅格地图。必须显式声明，不能依赖 git 的二进制自动识别：
# 那是靠内容里有没有 NUL 字节来判断的，而 PGM 的 NUL 恰好来自「占据」栅格
# （占据=0x00）。一张没有障碍物的地图就不含 NUL，会被误判成文本，
# 检出时插入 CR 把图片彻底损坏——而且直到 map_server 读图报错才会发现。
*.pgm binary
*.pgm -text
*.bmp binary
